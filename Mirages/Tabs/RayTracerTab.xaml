<UserControl x:Class="Mirages.Tabs.RayTracerTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:st="http://schemas.spicytaco.io/"
             mc:Ignorable="d">

    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="LightSteelBlue" BorderBrush="Gainsboro" BorderThickness="2">

            <st:StackPanel Margin="5" MarginBetweenChildren="5">

                <Button Content="Load" Command="{Binding Load}" IsEnabled="{Binding IsLoadEnabled}" />
                <Button Content="Unload" Command="{Binding Unload}" IsEnabled="{Binding IsUnloadEnabled}" />

            </st:StackPanel>
            
        </Border>
        
        <GroupBox Grid.Column="1" Header="Scene" Margin="0 0 10 0" Padding="5">
            <Canvas>
                <Image Source="{Binding Path=Image}" RenderOptions.BitmapScalingMode="NearestNeighbor" 
                       RenderOptions.EdgeMode="Aliased" Stretch="Fill"/>
            </Canvas>
        </GroupBox>

    </Grid>
    
</UserControl>
